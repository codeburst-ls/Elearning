export function set_jwt(state, jwt) {
  state.jwt = jwt;
}
export function set_school(state, school) {
  state.school = school;
}
export function set_user(state, user) {
  state.user = user;
}
export function set_isAuthenticated(state, isAuthenticated) {
  state.isAuthenticated = isAuthenticated;
}
